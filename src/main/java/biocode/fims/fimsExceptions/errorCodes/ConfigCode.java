package biocode.fims.fimsExceptions.errorCodes;

/**
 * {@link ErrorCode} to handle configuration errors
 */
public enum ConfigCode implements ErrorCode {
    UNKNOWN_ENTITY, MISSING_ATTRIBUTE, INVALID
}
