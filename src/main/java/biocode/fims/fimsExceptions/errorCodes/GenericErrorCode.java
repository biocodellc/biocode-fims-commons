package biocode.fims.fimsExceptions.errorCodes;

/**
 * {@link ErrorCode} to handle generic errors
 */
public enum GenericErrorCode implements ErrorCode {
    SERVER_ERROR, BAD_REQUEST, UNAUTHORIZED
}
