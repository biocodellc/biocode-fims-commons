package biocode.fims.fimsExceptions.errorCodes;

/**
 * @author RJ Ewing
 */
public enum DataReaderCode implements ErrorCode {
    MISSING_METADATA, READ_ERROR, DUPLICATE_COLUMNS, NO_DATA, NOT_FOUND, INVALID_RECORDS
}
